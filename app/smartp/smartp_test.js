'use strict';

describe('myApp.smartp module', function() {

  beforeEach(module('myApp.smartp'));

  describe('smartp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var smartpCtrl = $controller('SmartPCtrl');
      expect(smartpCtrl).toBeDefined();
    }));

  });
});